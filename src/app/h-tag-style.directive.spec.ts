import { HTagStyleDirective } from './h-tag-style.directive';

describe('HTagStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new HTagStyleDirective();
    expect(directive).toBeTruthy();
  });
});
