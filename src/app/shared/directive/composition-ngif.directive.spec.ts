import { CompositionNgifDirective } from './composition-ngif.directive';

describe('CompositionNgifDirective', () => {
  it('should create an instance', () => {
    const directive = new CompositionNgifDirective();
    expect(directive).toBeTruthy();
  });
});
